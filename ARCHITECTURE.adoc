= Documento de Arquitectura de Software

== Splitify

*Versión:* 2.4
*Fecha:* 2026-04-21
*Autor:* Barri

'''

== 1. Introducción

=== 1.1 Propósito

Este documento describe las decisiones arquitectónicas, la estructura del proyecto, los componentes del sistema y los flujos de integración de *Splitify*. Complementa el documento de requerimientos (REQUIREMENTS.adoc) definiendo *cómo* se implementan los requerimientos especificados.

=== 1.2 Alcance

Cubre la arquitectura del backend (Spring Boot), el frontend (React SPA), la integración entre ambos, las integraciones con APIs externas (Spotify, ChatGPT/OpenAI) y la estrategia de persistencia y despliegue.

'''

== 2. Visión General de la Arquitectura

Splitify sigue una arquitectura *cliente-servidor* con separación clara entre frontend y backend:

* *Frontend:* Single Page Application (SPA) desarrollada con React, JavaScript y Vite. Se comunica con el backend exclusivamente mediante una API REST.
* *Backend:* API REST desarrollada con Spring Boot (Java 25). Gestiona la lógica de negocio, la autenticación, la persistencia y la comunicación con APIs externas.
* *APIs externas:* Spotify Web API (autenticación, lectura/escritura de playlists, fechas de lanzamiento) y ChatGPT API (clasificación de género e idioma).

____

[source]
----
┌─────────────────────────────────────────────────────────────┐
│                        Cliente (Browser)                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              React SPA (Vite + JavaScript)             │  │
│  │  Páginas: Login │ Principal │ Detalle │ Crear Playlist│  │
│  └──────────────────────┬────────────────────────────────┘  │
└─────────────────────────┼───────────────────────────────────┘
                          │ HTTP (REST /api/**)
┌─────────────────────────┼───────────────────────────────────┐
│                   Spring Boot Backend                       │
│  ┌──────────────────────▼────────────────────────────────┐  │
│  │                  Controllers (REST)                   │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │                    Services                           │  │
│  │  PlaylistService │ ClassificationService │ SpotifyService│
│  ├───────────────────────────────────────────────────────┤  │
│  │                  Repositories (JPA)                   │  │
│  └──────────┬──────────────────┬─────────────────────────┘  │
│             │                  │                             │
│        ┌────▼────┐    ┌───────▼────────┐                    │
│        │   BD    │    │  APIs Externas  │                    │
│        │ SQLite/PG│    │ Spotify│ChatGPT │                    │
│        └─────────┘    └────────────────┘                    │
└─────────────────────────────────────────────────────────────┘
----

____

'''

== 3. Stack Tecnológico

|===
|Capa |Tecnología |Justificación

|Frontend |React 19 + JavaScript |Ecosistema maduro para SPAs, amplia comunidad
|Bundler |Vite |Hot Module Replacement rápido, build optimizado, proxy de desarrollo integrado
|CSS |Tailwind CSS |Framework de utilidades CSS; permite construir componentes con estilos directos sin escribir archivos CSS manuales
|Navegación |React Router v7 |Router estándar de la comunidad React para SPAs
|Backend |Spring Boot 4.0.4 (Java 25) |Framework estándar del ecosistema Java, integración nativa con OAuth2 y JPA
|Persistencia |Spring Data JPA |Abstracción sobre JPA para acceso a datos
|Base de datos |SQLite (dev y prod) |SQLite en ambos entornos. En prod se activa WAL mode (`journal_mode=WAL`) para permitir lecturas concurrentes sin bloquear escrituras; el volumen de escritura de Splitify (syncs ocasionales por usuario) cabe holgadamente sin necesidad de un motor cliente-servidor.
|Reverse proxy |Caddy v2 |Termina TLS (certificados Let's Encrypt automáticos, renovación transparente), reenvía a Spring Boot en `localhost:8080`, comprime respuestas con gzip.
|Gestor de servicios |systemd |Corre el JAR como servicio (`splitify.service`), reinicia automático ante crash, inyecta variables de entorno desde `/etc/splitify.env`, aplica hardening (`NoNewPrivileges`, `ProtectSystem`, etc).
|Autenticación |Spring Security + OAuth 2.0 |Integración directa con Spotify OAuth
|IA (clasificación) |OpenAI API + GPT-4.1-nano |API REST de OpenAI (via RestClient); GPT-4.1-nano es suficiente y económico para clasificar género e idioma
|Build |Gradle (Kotlin DSL) + plugin Node |Build unificado: Gradle orquesta tanto el backend como el frontend
|Despliegue |Linode |Servidor de despliegue objetivo (según requerimientos)
|===

'''

== 4. Estructura del Proyecto

[source]
----
splitify/
├── .github/
│   └── workflows/
│       └── deploy.yml                 ← CI/CD: auto-deploy a prod en cada push a master (GitHub Actions)
├── build.gradle.kts
├── settings.gradle.kts
├── deploy.sh                          ← Script de deploy manual: build JAR → scp → systemctl restart
├── frontend/                          ← Proyecto React (Vite)
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── main.jsx                   ← Punto de entrada React
│       ├── App.jsx                    ← Router principal (React Router v7)
│       ├── pages/                     ← Páginas/vistas
│       │   ├── LoginPage.jsx
│       │   ├── MainPage.jsx
│       │   └── PlaylistDetailPage.jsx
│       ├── components/                ← Componentes reutilizables
│       │   ├── Header.jsx             ← Header compartido (logo, perfil de usuario, logout)
│       │   ├── SyncOverlay.jsx        ← Modal bloqueante de sincronización
│       │   ├── PlaylistCard.jsx       ← Card de playlist con portada
│       │   ├── SplitifyCard.jsx       ← Card de playlist Splitify (rename inline, cambiar foto al hover, Actualizar/Eliminar)
│       │   ├── OrganizeModal.jsx      ← Modal de selección de criterios (idioma/género/fecha) + personalizado
│       │   ├── ConfirmPlaylistsModal.jsx ← Modal de confirmación con nombres editables antes de crear (RF6.1)
│       │   ├── CustomOrganizeModal.jsx ← Modal de creación personalizada (RF7)
│       │   ├── RefreshConfirmModal.jsx ← Modal de conflictos al actualizar: checkboxes por canción, agrupación por playlist (RF2.2)
│       │   └── ErrorModal.jsx         ← Modal genérico de error
│       └── services/                  ← Clientes HTTP para la API
│           └── api.js
├── src/main/
│   ├── java/io/jona/smusic/sorted_music/
│   │   ├── SortedMusicApplication.java
│   │   ├── config/                    ← Configuración (Security)
│   │   │   └── SecurityConfig.java
│   │   ├── controller/                ← Controllers (REST + Web)
│   │   │   ├── PlaylistController.java
│   │   │   ├── UserController.java
│   │   │   └── SpaController.java
│   │   ├── service/                   ← Lógica de negocio
│   │   │   ├── SpotifyService.java
│   │   │   └── ClassificationService.java ← Clasificación (ChatGPT: género e idioma)
│   │   ├── model/                     ← Entidades JPA
│   │   │   ├── Playlist.java         ← Playlist de Spotify (caché local)
│   │   │   ├── Song.java
│   │   │   ├── SongClassification.java ← Caché global de clasificaciones (género/idioma por spotifyId)
│   │   │   └── SplitifyPlaylistSource.java ← Relación Splitify playlist → playlists origen
│   │   ├── dto/                       ← Objetos de transferencia
│   │   │   ├── SpotifyDto.java       ← Records para deserializar Spotify API
│   │   │   ├── PlaylistDto.java
│   │   │   ├── SongDto.java
│   │   │   ├── UserProfileDto.java
│   │   │   ├── AvailableFiltersDto.java ← Idiomas/géneros/artistas disponibles (RF7)
│   │   │   ├── CreatePlaylistsRequest.java ← Criterios iniciales (idioma/género/fecha)
│   │   │   ├── CreatePlaylistsFromSpecsRequest.java ← Specs con nombres finales tras edición (RF6.1)
│   │   │   ├── PlaylistCreateSpec.java ← {filterType, filterValue, name} por playlist a crear
│   │   │   ├── CreateCustomPlaylistRequest.java ← Request de creación personalizada (RF7)
│   │   │   ├── RefreshRequest.java    ← Request con song IDs a restaurar (RF2.2)
│   │   │   ├── BatchRefreshRequest.java ← Request batch con IDs a restaurar por playlist
│   │   │   ├── BatchRefreshPreviewDto.java ← Preview por playlist en batch refresh
│   │   │   └── RefreshPreviewDto.java ← Vista previa de actualización (nuevas, eliminadas, cambios)
│   │   └── repository/               ← Repositorios JPA
│   │       ├── PlaylistRepository.java
│   │       ├── SongRepository.java
│   │       ├── SongClassificationRepository.java
│   │       └── SplitifyPlaylistSourceRepository.java
│   └── resources/
│       ├── application.properties           ← Config base (dev)
│       ├── application-prod.properties      ← Overrides de producción (SQLite en /opt/splitify, redirect URI https, forward-headers)
│       └── static/                    ← Build de React (generado por Vite)
└── src/test/
    └── java/...
----

'''

== 5. Arquitectura del Backend

=== 5.1 Capas

El backend sigue una arquitectura en capas:

* *Controller:* recibe las peticiones HTTP, valida la entrada y delega al servicio correspondiente. Todos los endpoints se exponen bajo `/api/**`.
* *Service:* contiene la lógica de negocio. Coordina la obtención de datos de Spotify, la clasificación con ChatGPT y la creación de playlists.
* *Repository:* acceso a datos mediante Spring Data JPA.

=== 5.2 Servicios Principales

|===
|Servicio |Responsabilidad

|`SpotifyService` |Servicio principal: comunicación con la Spotify Web API (sincronización, CRUD de playlists, obtención de canciones), lógica de negocio (filtrado por idioma/género/fecha, creación y actualización de playlists de Splitify), y persistencia. Gestiona la actualización inteligente de playlists: detecta cambios manuales en Spotify, mantiene estado de exclusiones y adds manuales, y reordena por fecha de lanzamiento.
|`ClassificationService` |Comunicación con la API de OpenAI (GPT-4.1-nano) para clasificar el género e idioma de cada canción. El idioma se detecta dinámicamente sin catálogo fijo. Utiliza caché global (`SongClassification`) para evitar re-clasificar canciones ya procesadas.
|===

=== 5.3 Endpoints de la API REST

|===
|Método |Ruta |Descripción |RF |Estado

|GET |`/api/user/me` |Obtener perfil del usuario (nombre y foto de Spotify) |RF1 |✅ Implementado
|POST |`/api/auth/logout` |Cerrar sesión |RF1 |✅ Implementado
|POST |`/api/playlists/sync` |Sincronizar playlists del usuario desde Spotify (operación bloqueante) |RF1.1 |✅ Implementado
|GET |`/api/playlists` |Obtener todas las playlists del usuario |RF2 |✅ Implementado
|GET |`/api/playlists/{id}` |Obtener una playlist por ID |RF2 |✅ Implementado
|GET |`/api/playlists/{id}/songs` |Obtener canciones de una playlist con metadatos (género, idioma, año) |RF2, RF3 |✅ Implementado (sin paginación aún)
|POST |`/api/playlists/create/combined` |Crear playlists organizadas por idioma, género y/o fecha con nombres por defecto (camino directo, sin preview) |RF4, RF5, RF6, RF6.1 |✅ Implementado
|POST |`/api/playlists/create/combined/preview` |Calcular qué playlists se generarían con los criterios elegidos y devolver la lista de specs con nombres por defecto editables por el usuario |RF6.1 |✅ Implementado
|POST |`/api/playlists/create/combined/confirm` |Crear las playlists a partir de una lista de specs (con nombres finales tras edición del usuario) |RF6.1 |✅ Implementado
|GET |`/api/playlists/available-filters?playlistIds={ids}` |Obtener idiomas, géneros y artistas disponibles en las playlists seleccionadas |RF7 |✅ Implementado
|POST |`/api/playlists/create/custom` |Crear playlist personalizada con filtros combinados (persiste los filtros en `customLanguages`/`customGenres`/`customArtists`) |RF7 |✅ Implementado
|GET |`/api/playlists/splitify/{id}/refresh/preview` |Vista previa de actualización: detecta canciones nuevas de fuentes, canciones eliminadas manualmente, y canciones agregadas manualmente |RF2.2 |✅ Implementado
|POST |`/api/playlists/splitify/batch/refresh/preview` |Preview de actualización para un lote de playlists. Devuelve `{playlistId, playlistName, preview}` para que el frontend pueda agrupar conflictos por playlist |RF2.1, RF2.2 |✅ Implementado
|PUT |`/api/playlists/splitify/{id}/refresh` |Actualizar una playlist de Splitify. Body: `{restoredSongIds: [...]}` — lista explícita de spotifyIds de canciones a restaurar. Lee estado real de Spotify, re-evalúa fuentes, sincroniza nombre/imagen, reordena por fecha, reemplaza contenido |RF2, RF2.2 |✅ Implementado
|PUT |`/api/playlists/splitify/batch/refresh` |Actualizar múltiples playlists de Splitify en lote. Body: `{items: [{playlistId, restoredSongIds}]}` — lista de selecciones de restauración por playlist |RF2.1, RF2.2 |✅ Implementado
|PUT |`/api/playlists/splitify/{id}/rename` |Renombrar una playlist de Splitify. Body: `{name}`. Cambia el nombre en Spotify y BD |RF2.3 |✅ Implementado
|PUT |`/api/playlists/splitify/{id}/image` |Cambiar la foto de una playlist de Splitify. Body: `{imageBase64}` (JPEG base64, máx 256KB). Sube a Spotify y guarda la URL nueva |RF2.3 |✅ Implementado
|DELETE |`/api/playlists/splitify/{id}` |Eliminar una playlist de Splitify |RF2 |✅ Implementado
|DELETE |`/api/playlists/splitify` |Eliminar múltiples playlists de Splitify |RF2.1 |✅ Implementado
|GET |`/api/playlists/splitify` |Obtener playlists creadas por Splitify |RF2 |✅ Implementado
|===

'''

== 6. Arquitectura del Frontend

=== 6.1 SPA y Navegación

El frontend es una Single Page Application con navegación del lado del cliente gestionada por *React Router v7*. Spring Boot sirve el `index.html` generado por Vite para cualquier ruta que no comience con `/api/`. La navegación entre páginas no recarga el navegador. Las rutas autenticadas (`/` y `/playlist/:id`) comparten un layout (`AuthLayout` en `App.jsx`) que contiene el componente `Header` reutilizable y gestiona la obtención del perfil del usuario y el cierre de sesión.

=== 6.2 Páginas Principales

|===
|Página |Ruta |Descripción

|Login |`/login` |Botón de inicio de sesión con Spotify
|Principal |`/` |Estado vacío con botón "Sincronizar Playlists" si no hay playlists sincronizadas. Una vez sincronizadas: sección superior (playlists del usuario con selección) y sección inferior (playlists de Splitify con acciones individuales y en lote)
|Sincronizando |`/` (modal bloqueante) |Pantalla bloqueante que se muestra durante la sincronización de playlists, impidiendo cualquier interacción
|Detalle de playlist |`/playlist/:id` |Lista de canciones con metadatos (nombre, artista, año, género, idioma) con carga paginada por scroll (infinite scroll)
|Crear playlist personalizada |`/create` |Formulario con filtros dinámicos de idioma, género y artista. Solo muestra los filtros correspondientes a idiomas, géneros y artistas presentes en las canciones de las playlists seleccionadas (RF7)
|===

=== 6.3 Estilos

La interfaz utiliza *Tailwind CSS* como framework de utilidades. No se emplea una librería de componentes pre-construidos; todos los componentes se construyen manualmente con clases de utilidad de Tailwind, lo que permite un control total sobre el diseño.

=== 6.4 Comunicación con el Backend

Todas las llamadas al backend se realizan mediante `fetch`, apuntando a `/api/**`. En desarrollo, Vite redirige estas peticiones al servidor de Spring Boot mediante su proxy integrado.

'''

== 7. Integraciones Externas

=== 7.1 Spotify Web API

|===
|Operación |Endpoint de Spotify |Uso en el sistema

|Autenticación |OAuth 2.0 Authorization Code Flow (con PKCE) |Inicio de sesión del usuario (RF1)
|Obtener perfil |`GET /me` |Obtener nombre y foto de perfil del usuario para mostrar en el header (RF1)
|Obtener playlists |`GET /me/playlists` |Listar playlists del usuario (RF2)
|Obtener Liked Songs |`GET /me/tracks` |Incluir Liked Songs como playlist seleccionable (RF2)
|Obtener canciones |`GET /playlists/{id}/items` |Leer canciones para clasificación y filtrado (RF3). Renombrado de `/tracks` a `/items` en feb 2026.
|Crear playlist |`POST /me/playlists` |Generar nuevas playlists (RF4–RF7). Renombrado de `/users/{id}/playlists` a `/me/playlists` en feb 2026.
|Agregar canciones |`POST /playlists/{id}/items` |Poblar las playlists creadas (RF4–RF7). Renombrado de `/tracks` a `/items` en feb 2026.
|Reemplazar canciones |`PUT /playlists/{id}/items` |Reemplazar el contenido completo de una playlist (usado al actualizar para reordenar, RF2.2).
|Obtener playlist única |`GET /playlists/{id}` |Leer metadata (nombre, imagen) de una playlist de Splitify tras crear/renombrar/subir imagen (RF2.2, RF2.3, RF6.1).
|Renombrar playlist |`PUT /playlists/{id}` |Actualizar el nombre de una playlist de Splitify (RF2.3).
|Subir imagen de playlist |`PUT /playlists/{id}/images` |Subir portada JPEG en base64 (máx 256KB) para playlists de Splitify (RF2.3). Requiere scope `ugc-image-upload`.
|Eliminar playlist |`DELETE /me/library` (con URI `spotify:playlist:{id}` en body) |Eliminar playlists de Splitify (RF2, RF2.1). Renombrado de `DELETE /playlists/{id}/followers` en feb 2026.
|===

==== 7.1.1 Restricciones del Modo Desarrollo de Spotify

Desde febrero 2026, Spotify impone restricciones en apps con *Development Mode*:

* El endpoint `GET /playlists/{id}/items` solo devuelve canciones de playlists que el usuario **creó o colabora**. Playlists de otros usuarios (que solo se siguen) devuelven 403 Forbidden.
* El endpoint `POST /users/{id}/playlists` fue reemplazado por `POST /me/playlists`.
* El endpoint `POST /playlists/{id}/tracks` fue renombrado a `POST /playlists/{id}/items`.
* El campo `track` dentro de cada item fue renombrado a `item` en la respuesta JSON.
* El campo `tracks` en el objeto playlist fue renombrado a `items`.
* Máximo 5 usuarios autorizados (deben agregarse manualmente en el Developer Dashboard).
* Se requiere cuenta Premium para el propietario de la app.

Para acceso completo (incluyendo playlists de otros usuarios), se debe solicitar *Extended Quota Mode* en el Spotify Developer Dashboard (requiere aprobación manual de Spotify).

=== 7.2 API de Clasificación (OpenAI)

El servicio `ClassificationService` se comunica con la API REST de OpenAI mediante `RestClient` de Spring. El modelo utilizado es *GPT-4.1-nano*, seleccionado por ser suficiente para tareas de categorización y por su bajo costo. ChatGPT se utiliza *exclusivamente* para clasificar género e idioma (no para fechas de lanzamiento).

El servicio envía el nombre y artista de cada canción para obtener:

* *Género:* clasificado en una de las 10 categorías definidas en el SRS (sección 3.2).
* *Idioma(s):* detectados dinámicamente sin limitarse a un catálogo fijo. ChatGPT identifica el/los idioma(s) de la canción. Una canción puede tener múltiples idiomas si se canta en más de uno (ej: una canción con partes en inglés y español devuelve ambos idiomas). Las canciones instrumentales se clasifican como "Sin letra".

Para optimizar costos y rendimiento, las clasificaciones se almacenan en caché en la base de datos. Si una canción ya fue clasificada previamente, no se consulta la API nuevamente.

'''

== 8. Modelo de Datos

=== 8.1 Entidades

[source]
----
Playlist                                  ← Playlists del usuario (caché local de Spotify)
├── id: Long (PK, autogenerado)
├── spotifyId: String                     ← ID de Spotify, o "liked_songs" para Liked Songs
├── name: String
├── imageUrl: String (nullable)           ← URL de la portada (sincronizada con Spotify en refresh/sync)
├── totalTracks: int                      ← Cantidad total de canciones
├── userId: String                        ← ID del usuario de Spotify
├── splitify: boolean (default false)     ← true si fue creada por Splitify
├── snapshotId: String (nullable)         ← Snapshot ID de Spotify para sync incremental
├── filterType: String (nullable)         ← Criterio usado al crear (solo splitify): "language", "genre", "releaseDate", "custom"
├── filterValue: String (nullable)        ← Valor del filtro (solo splitify, casos simples): ej "Rock", "Inglés"; null para releaseDate y custom
├── customLanguages: TEXT (nullable)      ← Solo cuando filterType="custom": JSON array de idiomas elegidos
├── customGenres: TEXT (nullable)         ← Solo cuando filterType="custom": JSON array de géneros elegidos
└── customArtists: TEXT (nullable)        ← Solo cuando filterType="custom": JSON array de artistas elegidos

Song                                      ← Canciones de cada playlist
├── id: Long (PK, autogenerado)
├── spotifyId: String                     ← ID único de Spotify
├── name: String
├── artist: String                        ← Artista(s) separados por coma
├── releaseYear: Integer (nullable)       ← Año de lanzamiento del álbum en Spotify
├── releaseDate: String (nullable)        ← Fecha completa para ordenación interna ("YYYY-MM-DD", "YYYY-MM" o "YYYY")
├── genre: String (nullable)              ← Género musical (ChatGPT)
├── language: String (nullable)           ← Idioma(s) separados por coma (ChatGPT)
├── excluded: boolean (default false)     ← true si el usuario eligió no restaurar esta canción (solo en playlists Splitify)
├── manuallyAdded: boolean (default false) ← true si el usuario agregó esta canción manualmente en Spotify (solo en playlists Splitify)
└── playlist_id: Long (FK → Playlist)     ← ManyToOne

SongClassification                        ← Caché global de clasificaciones (evita re-consultar ChatGPT)
├── id: Long (PK, autogenerado)
├── spotifyId: String (unique, not null)  ← ID de Spotify de la canción
├── genre: String (nullable)              ← Género clasificado
└── language: String (nullable)           ← Idioma(s) clasificados

SplitifyPlaylistSource                    ← Relación entre playlists de Splitify y sus playlists origen
├── id: Long (PK, autogenerado)
├── splitifyPlaylistId: Long (FK → Playlist)  ← Playlist de Splitify
└── sourcePlaylistId: Long (FK → Playlist)    ← Playlist origen del usuario
----

La tabla `SplitifyPlaylistSource` permite al sistema saber qué playlists origen se usaron para crear cada playlist de Splitify, lo cual es necesario para la funcionalidad de actualización (RF2.2). Junto con los campos `filterType` y `filterValue` de la entidad `Playlist`, el sistema puede re-evaluar las canciones de las fuentes y aplicar el mismo criterio de organización original.

=== 8.2 Estrategia de Persistencia

* La entidad `Playlist` persiste las playlists del usuario de Spotify localmente tras la sincronización. Al re-sincronizar, se eliminan los datos viejos y se reemplazan con datos frescos de Spotify.
* La entidad `Song` almacena todas las canciones de cada playlist. Una misma canción de Spotify puede existir múltiples veces si aparece en varias playlists (relación ManyToOne simple). La sincronización trae todas las canciones de cada playlist (incluida Liked Songs) paginando la API de Spotify con `limit=50` y siguiendo el campo `next` de cada respuesta hasta agotar los resultados. Los campos `releaseYear` y `releaseDate` se obtienen directamente de Spotify (fecha del álbum). Los campos `genre` y `language` se clasifican mediante ChatGPT API (GPT-4.1-nano). En playlists de Splitify, los campos `excluded` y `manuallyAdded` rastrean el estado de cada canción: `excluded=true` indica que el usuario eligió no restaurar la canción (se recuerda permanentemente); `manuallyAdded=true` indica que el usuario la agregó directamente en Spotify (se preserva aunque no esté en las fuentes). Canciones con `excluded=true` no se muestran en la vista de detalle ni se agregan a Spotify.
* La entidad `SongClassification` funciona como caché global de clasificaciones. Almacena el género e idioma clasificados por ChatGPT indexados por `spotifyId`. Al re-sincronizar, las clasificaciones existentes se preservan y solo se consulta ChatGPT para canciones nuevas.
* La entidad `SplitifyPlaylistSource` registra la relación entre cada playlist de Splitify y sus playlists origen. Junto con los campos `filterType` y `filterValue` de `Playlist`, esta tabla permite al sistema re-evaluar las canciones de las fuentes y aplicar el mismo criterio original durante la actualización (RF2.2).
* Liked Songs se almacena como una playlist con `spotifyId = "liked_songs"` e `imageUrl = null`. La consulta de playlists ordena siempre Liked Songs primero mediante `ORDER BY CASE WHEN spotifyId = 'liked_songs' THEN 0 ELSE 1 END, id ASC`.

'''

== 9. Autenticación y Seguridad

=== 9.1 Flujo de Autenticación

[source]
----
Usuario ──▶ [Botón "Iniciar sesión"] ──▶ Spotify OAuth
                                              │
                                    (autoriza permisos)
                                              │
Spotify ──▶ [Redirect con código] ──▶ Backend (Spring Security)
                                              │
                                    (intercambia código por token)
                                              │
Backend ──▶ [Sesión autenticada] ──▶ Frontend (acceso a /api/**)
----

=== 9.2 Reglas de Seguridad

|===
|Ruta |Acceso

|`/api/**` |Solo usuarios autenticados
|`/api/auth/status` |Público (para verificar estado de sesión)
|`/login`, `/oauth2/**` |Público (flujo OAuth)
|Cualquier otra ruta |Público (sirve el SPA)
|===

=== 9.3 CSRF y CORS

* *CORS:* En desarrollo, el proxy de Vite redirige las peticiones `/api/**` al backend, por lo que el navegador las ve como mismo origen y no se genera conflicto de CORS. En producción, frontend y backend se sirven desde el mismo JAR (mismo origen), por lo que CORS no aplica. No se requiere configuración adicional de CORS.
* *CSRF:* La protección CSRF está deshabilitada para las rutas `/api/**`. Dado que la SPA y la API se sirven desde el mismo origen (mismo JAR en producción, proxy en desarrollo) y la sesión usa cookies con `SameSite=Lax`, el riesgo de CSRF es mínimo. Esto simplifica la comunicación frontend-backend al no requerir envío de tokens CSRF.

=== 9.4 Permisos de Spotify (Scopes)

|===
|Scope |Necesario para |Estado

|`user-read-private` |Leer perfil del usuario |✅ Configurado
|`user-read-email` |Leer email del usuario |✅ Configurado
|`user-library-read` |Acceder a Liked Songs |✅ Configurado
|`playlist-read-private` |Leer playlists privadas del usuario |✅ Configurado
|`playlist-read-collaborative` |Leer playlists colaborativas |✅ Configurado
|`playlist-modify-public` |Crear y modificar playlists públicas |✅ Configurado
|`playlist-modify-private` |Crear y modificar playlists privadas |✅ Configurado
|`user-library-modify` |Eliminar playlists de la biblioteca del usuario |✅ Configurado
|`ugc-image-upload` |Subir imágenes de portada para playlists de Splitify (RF2.3) |✅ Configurado
|===

'''

== 10. Integración Frontend-Backend (Build y Desarrollo)

=== 10.1 Desarrollo Local

En desarrollo se ejecutan dos procesos en paralelo:

* `./gradlew bootRun` — Backend en el puerto *8080*
* `npm run dev` (dentro de `frontend/`) — Vite dev server en el puerto *5173* con Hot Module Replacement

Vite está configurado con un proxy que redirige las peticiones `/api/**` al puerto 8080, eliminando problemas de CORS durante el desarrollo (ver sección 9.3).

=== 10.2 Build de Producción

Gradle orquesta el build completo mediante el plugin `com.github.node-gradle.node`:

[source]
----
./gradlew build
    │
    ├── 1. npm install (frontend/)
    ├── 2. npm run build (frontend/)
    │       └── Vite genera archivos en src/main/resources/static/
    ├── 3. Compilación Java
    └── 4. Empaquetado JAR
            └── JAR único contiene backend + frontend estático
----

El resultado es un *único archivo JAR* que sirve tanto la API como el frontend estático.

=== 10.3 Configuración de Vite

El archivo `frontend/vite.config.js` define:

* *`build.outDir`*: `../src/main/resources/static` — deposita el build donde Spring Boot sirve archivos estáticos.
* *`server.proxy`*: redirige `/api` al backend durante desarrollo.

=== 10.4 SPA Forwarding

Un controller en Spring Boot reenvía todas las rutas no-API al `index.html`, permitiendo que React maneje la navegación del lado del cliente. Se utilizan dos mappings: uno para rutas de un segmento (`/login`) y otro para rutas anidadas (`/playlist/123`), excluyendo rutas de assets, API y OAuth:

[source,java]
----
@Controller
public class SpaController {
    @GetMapping(value = "/{path:[^\\.]*}")
    public String forward() {
        return "forward:/index.html";
    }

    @GetMapping(value = "/{path:(?!api|assets|oauth2|login\\/oauth2)[^\\.]*}/**")
    public String forwardNested() {
        return "forward:/index.html";
    }
}
----

'''

== 11. Despliegue

=== 11.1 Topología de Producción

[source]
----
                                  Internet
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  Cloudflare DNS only  │   ← A records @ y www
                          │  splitifyapp.app      │     apuntan a la IP de Linode
                          └───────────┬───────────┘     (proxy naranja OFF)
                                      │
                                      ▼
                         ┌─────────────────────────┐
                         │  Linode Cloud Firewall  │   ← permite solo 22, 80, 443
                         │  (default inbound DROP) │     bloquea todo lo demás
                         └───────────┬─────────────┘
                                     │
       ┌─────────────────────────────┼─────────────────────────────────┐
       │                       Linode Nanode 1GB                       │
       │                       Ubuntu 24.04 LTS                        │
       │                                                               │
       │                     ┌───────▼────────┐                        │
       │        puertos 80/443│     Caddy     │  ← HTTPS via Let's Encrypt
       │                     │  (systemd)     │    HTTP-01 challenge
       │                     │  Caddyfile     │    reverse_proxy + gzip
       │                     └───────┬────────┘                        │
       │                             │ HTTP localhost:8080             │
       │                             ▼                                 │
       │               ┌──────────────────────────┐                    │
       │               │  splitify.service        │   user: splitify   │
       │               │  java -jar splitify.jar  │   env desde        │
       │               │  profile=prod            │   /etc/splitify.env│
       │               │  (systemd)               │                    │
       │               └──────────┬───────────────┘                    │
       │                          │ JDBC                               │
       │                          ▼                                    │
       │          ┌──────────────────────────────────┐                 │
       │          │  /opt/splitify/splitify.db       │                 │
       │          │  SQLite WAL mode                 │                 │
       │          └──────────────────────────────────┘                 │
       │                                                               │
       │   Outbound → Spotify Web API, OpenAI API (classification)     │
       └───────────────────────────────────────────────────────────────┘
----

=== 11.2 Infraestructura

|===
|Componente |Detalle

|Hosting |Linode Nanode 1GB (1 vCPU, 1GB RAM, 25GB SSD), Ubuntu 24.04 LTS
|DNS |Cloudflare (registros `A` para `@` y `www` → IP pública de la Linode, con proxy en modo "DNS only" / nube gris para que Let's Encrypt pueda responder el HTTP-01 challenge)
|Firewall |Linode Cloud Firewall `splitify-fw` con política por defecto *Drop* inbound y reglas *Accept* para TCP 22 (SSH), 80 (HTTP) y 443 (HTTPS). Outbound accept por defecto (necesario para Spotify API, OpenAI API, apt, Let's Encrypt).
|Dominio |`splitifyapp.app` + `www.splitifyapp.app` — el TLD `.app` está en HSTS preload, por lo que los navegadores fuerzan HTTPS automáticamente.
|===

=== 11.3 Hardening del Servidor

* *Usuario de servicio:* la app corre como usuario no privilegiado `splitify` (miembro del grupo `sudo` con `NOPASSWD` declarado en `/etc/sudoers.d/splitify` — válido porque el acceso SSH es exclusivamente por llave).
* *SSH:* en `/etc/ssh/sshd_config` y `/etc/ssh/sshd_config.d/*.conf` se deshabilita `PasswordAuthentication` y `PermitRootLogin`, y se exige `PubkeyAuthentication`. Las llaves públicas autorizadas están en `/home/splitify/.ssh/authorized_keys` (permisos `600`, owner `splitify`).
* *Hardening del servicio systemd:* la unit `splitify.service` aplica:
** `User=splitify`, `Group=splitify` — evita ejecución como root.
** `NoNewPrivileges=true` — no puede escalar privilegios vía setuid.
** `PrivateTmp=true` — `/tmp` y `/var/tmp` aislados.
** `ProtectSystem=full` — `/usr`, `/boot`, `/etc` son read-only.
** `ProtectHome=read-only` — los `/home` son read-only desde el servicio.
** `ReadWritePaths=/opt/splitify` — única ruta escribible, donde vive el JAR y la DB.

=== 11.4 Perfiles de Configuración

|===
|Perfil |Propiedades que sobreescribe |Uso

|(default / dev) |`application.properties` base: SQLite `splitify.db` relativo al working dir, redirect URI `http://127.0.0.1:8080/...` |Desarrollo local
|`prod` |`application-prod.properties`: SQLite en `/opt/splitify/splitify.db?journal_mode=WAL&synchronous=NORMAL`, redirect URI `https://splitifyapp.app/login/oauth2/code/spotify`, `server.forward-headers-strategy=native` (para que Spring confíe en `X-Forwarded-Proto` de Caddy y genere URLs OAuth con `https://`) |Producción en Linode
|===

El perfil activo se fija vía `SPRING_PROFILES_ACTIVE=prod` declarado en `/etc/splitify.env` (cargado por systemd como `EnvironmentFile`).

=== 11.5 Variables de Entorno (Producción)

Ubicación: `/etc/splitify.env` (permisos `640`, owner `root:splitify`; nunca en el JAR ni en el repo).

|===
|Variable |Descripción

|`SPOTIFY_CLIENT_ID` |Client ID de la app registrada en Spotify Developer Dashboard
|`SPOTIFY_CLIENT_SECRET` |Client Secret de Spotify
|`OPENAI_API_KEY` |API key para ChatGPT (clasificación de género e idioma)
|`SPRING_PROFILES_ACTIVE` |Fijado a `prod`
|===

=== 11.6 Archivos Clave en el Servidor

|===
|Ruta |Propósito |Permisos

|`/opt/splitify/splitify.jar` |JAR ejecutable (backend + frontend estático) |`644 splitify:splitify`
|`/opt/splitify/splitify.db` |Base SQLite en WAL mode (creada al primer arranque) |`644 splitify:splitify`
|`/etc/splitify.env` |Secretos y profile activo |`640 root:splitify`
|`/etc/systemd/system/splitify.service` |Unit de systemd |`644 root:root`
|`/etc/caddy/Caddyfile` |Config de Caddy (reverse proxy + HTTPS automático) |`644 root:root`
|===

=== 11.7 Caddy y HTTPS Automático

`/etc/caddy/Caddyfile`:

[source]
----
{
    email hofmanndavid@gmail.com
}

splitifyapp.app, www.splitifyapp.app {
    reverse_proxy localhost:8080
    encode gzip
}
----

Al recibir el primer request, Caddy:

1. Detecta que no tiene certificado para `splitifyapp.app`.
2. Inicia el flujo ACME contra Let's Encrypt.
3. Resuelve el challenge HTTP-01 sirviéndose a sí mismo en el puerto 80 (por eso el puerto 80 debe estar abierto en el Cloud Firewall y sin proxy de Cloudflare interceptando).
4. Obtiene el certificado, lo almacena en `/var/lib/caddy/` y lo usa para TLS.
5. Los renueva solo (~30 días antes del vencimiento).

El puerto 8080 de Spring Boot *no* está expuesto al exterior: solo Caddy, corriendo en el mismo host, lo puede alcanzar vía `localhost`.

=== 11.8 Runbook de Deploy

El script `deploy.sh` en la raíz del repo automatiza todo el ciclo:

[source,bash]
----
#!/usr/bin/env bash
set -euo pipefail

SERVER="splitify@192.155.93.19"
REMOTE_JAR="/opt/splitify/splitify.jar"
LOCAL_JAR="build/libs/sorted-music-0.0.1-SNAPSHOT.jar"

./gradlew clean build -x test                 # 1. build JAR (frontend+backend)
scp "$LOCAL_JAR" "$SERVER:$REMOTE_JAR"        # 2. sube el JAR al server
ssh "$SERVER" "sudo systemctl restart splitify"  # 3. reinicia el servicio
ssh "$SERVER" "sudo systemctl status splitify --no-pager -l | head -n 20"
----

Uso: `./deploy.sh` desde la raíz del proyecto. Tarda ~1–3 minutos (el build es lo más pesado).

=== 11.9 Runbook de Troubleshooting

|===
|Síntoma |Diagnóstico

|502 Bad Gateway en el browser |Spring Boot no está corriendo o crasheó. Ver logs: `sudo journalctl -u splitify -n 200 --no-pager`
|Error de certificado / HTTPS rompe |Verificar DNS (`nslookup splitifyapp.app 1.1.1.1` debe devolver la IP de la Linode, no Cloudflare). Verificar que el puerto 80 esté abierto en el Cloud Firewall. Ver logs de Caddy: `sudo journalctl -u caddy -n 100 --no-pager`
|OAuth de Spotify devuelve error `redirect_uri_mismatch` |El redirect URI de prod no está registrado en Spotify Developer Dashboard o no coincide. Debe existir `https://splitifyapp.app/login/oauth2/code/spotify`.
|OAuth devuelve "invalid client" o redirige a `http://` |Falta `server.forward-headers-strategy=native` en `application-prod.properties`, o el profile `prod` no se activó. Verificar `sudo systemctl show splitify -p Environment`.
|La app arranca pero no persiste datos |Verificar permisos de `/opt/splitify/` (debe permitir escritura al usuario `splitify`) y que el `ReadWritePaths` de systemd lo incluya.
|El deploy termina OK pero el browser muestra la versión vieja |Cache del navegador o del service worker. Hacer *hard reload* (`Ctrl+Shift+R` / `Ctrl+F5`) o abrir la app en una pestaña incógnita. Confirmar que realmente se deployó mirando el uptime del servicio con `sudo systemctl status splitify` (el `Active: active (running) since ...` debe ser reciente).
|===

=== 11.10 Rollback

Dado que el deploy sobreescribe `/opt/splitify/splitify.jar`, el rollback consiste en volver a construir y desplegar una versión previa del código (git checkout al commit anterior + `./deploy.sh`, o `git revert` + push para reutilizar CI/CD). No hay snapshotting automático del JAR; si se requiere rollback inmediato, mantener una copia manual con timestamp antes de cada deploy, ej.:

[source,bash]
----
ssh splitify@<IP> "sudo cp /opt/splitify/splitify.jar /opt/splitify/splitify.jar.$(date +%Y%m%d-%H%M%S)"
----

=== 11.11 CI/CD (Auto-Deploy con GitHub Actions)

El archivo `.github/workflows/deploy.yml` define un pipeline que se ejecuta *automáticamente en cada push a `master`* (y se puede disparar manualmente desde la UI de GitHub con `workflow_dispatch`). Esto reemplaza al `deploy.sh` manual en el flujo diario: con hacer `git push`, el código llega a producción sin pasos adicionales.

==== 11.11.1 Pipeline

[source]
----
git push (master)
       │
       ▼
┌──────────────────────────┐
│   GitHub Actions runner  │  (ubuntu-latest, efímero)
│                          │
│   1. checkout            │
│   2. setup JDK 25 Temurin│
│   3. setup Node 20       │
│   4. ./gradlew build -x test   ← genera el JAR
│   5. ssh-agent + SSH key │
│   6. ssh-keyscan server  │
│   7. scp JAR → /opt/splitify/splitify.jar
│   8. ssh sudo systemctl restart splitify
│   9. ssh systemctl is-active   ← health check
└──────────────────────────┘
       │
       ▼
  Linode server actualizado
----

==== 11.11.2 Configuración

*Secretos del repo* (GitHub → Settings → Secrets and variables → Actions):

|===
|Secreto |Valor

|`SSH_PRIVATE_KEY` |Contenido completo de una llave privada SSH *dedicada para CI* (distinta de la personal del desarrollador). Generada con `ssh-keygen -t ed25519 -f ~/.ssh/github_actions_splitify -N ""` sin passphrase (el runner no puede responder interactivamente).
|`SSH_HOST` |IP pública o dominio del server (ej: `192.155.93.19`).
|`SSH_USER` |`splitify` (el usuario de servicio creado en Fase 1 del hardening).
|===

*Lado del server:* la llave pública correspondiente (`~/.ssh/github_actions_splitify.pub`) debe estar agregada en `/home/splitify/.ssh/authorized_keys`. Se puede revocar en cualquier momento quitando esa línea — el deploy manual con `deploy.sh` sigue funcionando porque usa la llave personal del desarrollador.

==== 11.11.3 Propiedades del pipeline

* *Concurrencia:* `concurrency: deploy-prod` con `cancel-in-progress: false` — si hay una corrida en curso y llega otra, esta espera a que termine la primera en vez de pisarla. Evita deploys en carrera.
* *Timeout:* 15 minutos (el build + scp + restart tarda ~3–5 min, el resto es margen).
* *Dispara en:* push a `master` y manual (`workflow_dispatch`).
* *Fallo:* si cualquier paso falla (build, scp, restart, health check), el workflow queda en rojo y GitHub notifica por mail. El server queda con el JAR previo aún corriendo (systemd solo hace restart si el scp tuvo éxito); si el restart falla pero el servicio quedó detenido, se diagnostica con `sudo journalctl -u splitify -n 200`.
* *Auditoría:* cada deploy queda en la pestaña Actions del repo, con logs, duración y el commit que lo disparó.

==== 11.11.4 Convivencia con `deploy.sh`

`deploy.sh` sigue existiendo y sirve para casos de emergencia en los que GitHub Actions esté caído o se necesite deployar una versión local no commiteada (ej: probar un hotfix sin pasar por el repo). El workflow de CI usa exactamente los mismos pasos, así que ambos caminos producen el mismo resultado.

=== 11.12 Runbook de Provisioning (First-Time Setup)

Esta sección documenta *todos* los pasos y comandos necesarios para levantar el stack de producción desde cero en una máquina nueva. Está pensado como referencia para replicar el setup, auditar la configuración actual, o recuperar tras una pérdida total del server.

==== 11.12.1 Pre-requisitos en la máquina del desarrollador

Una llave SSH para autenticarse contra el server. Si no existe:

[source,bash]
----
ssh-keygen -t ed25519 -C "tu@email.com"
# Aceptar la ruta por defecto (~/.ssh/id_ed25519). Passphrase opcional.
cat ~/.ssh/id_ed25519.pub   # copiar esta línea para pegarla más tarde
----

==== 11.12.2 Crear el Cloud Firewall en Linode

Desde `https://cloud.linode.com/firewalls/create`:

* Label: `splitify-fw`
* Default Inbound Policy: *Drop*
* Default Outbound Policy: *Accept*
* Services assigned (shortcut que genera las reglas): *SSH*, *HTTP*, *HTTPS*

Equivalente a tres reglas inbound Accept TCP en puertos 22, 80 y 443 desde `0.0.0.0/0, ::/0`.

==== 11.12.3 Crear la instancia Linode

Desde `https://cloud.linode.com/linodes/create`:

* Distribution: *Ubuntu 24.04 LTS*
* Region: la más cercana geográficamente
* Plan: *Nanode 1GB* (Shared CPU)
* Label: `splitify-prod`
* Root Password: fuerte (mín 11 chars, mayúscula+minúscula+número+símbolo), guardada en un password manager
* SSH Keys: pegar la pública copiada en 11.12.1
* Firewall: `splitify-fw`

Tras ~1 min queda en estado *Running* con una IP pública asignada (anotarla: la llamamos `<IP>`).

==== 11.12.4 Configurar DNS en Cloudflare

En el panel del dominio en Cloudflare → *DNS → Records* → *Add record* dos veces:

[cols="1,1,1,1"]
|===
|Type |Name |Content |Proxy

|A |`@` |`<IP>` |*DNS only* (nube gris)
|A |`www` |`<IP>` |*DNS only* (nube gris)
|===

*Crítico:* el proxy de Cloudflare (nube naranja) debe estar *apagado*. Con el proxy activo, Cloudflare intercepta el puerto 80 y Caddy no puede completar el HTTP-01 challenge de Let's Encrypt. Verificar la propagación (puede tardar 1-5 min):

[source,bash]
----
nslookup splitifyapp.app 1.1.1.1
# Debe devolver <IP>, no una IP de Cloudflare (104.x, 172.x)
----

==== 11.12.5 Primer acceso SSH y actualización del sistema

[source,bash]
----
ssh root@<IP>
# Primera vez pregunta por host key → "yes"
# Pide password de root (el configurado al crear la Linode)
----

Ya dentro del server como `root@localhost`:

[source,bash]
----
apt update && apt upgrade -y
# Si pregunta qué servicios reiniciar: Enter con la opción por defecto.
# Si pregunta sobre archivos de config: "keep the local version".
----

==== 11.12.6 Crear el usuario de servicio `splitify`

[source,bash]
----
adduser --disabled-password --gecos "" splitify
usermod -aG sudo splitify
----

* `--disabled-password` crea el usuario sin password (solo entrará por SSH key).
* `-aG sudo` lo agrega al grupo `sudo` (permiso de elevar privilegios).

Copiar la llave SSH pública del desarrollador al nuevo usuario:

[source,bash]
----
mkdir -p /home/splitify/.ssh
echo "ssh-ed25519 AAAA... tu@email.com" > /home/splitify/.ssh/authorized_keys
chown -R splitify:splitify /home/splitify
chmod 700 /home/splitify/.ssh
chmod 600 /home/splitify/.ssh/authorized_keys
----

*Nota:* reemplazar `ssh-ed25519 AAAA... tu@email.com` por la línea completa copiada en 11.12.1.

Habilitar sudo sin password (seguro porque SSH solo acepta llaves):

[source,bash]
----
echo "splitify ALL=(ALL) NOPASSWD:ALL" | tee /etc/sudoers.d/splitify
chmod 0440 /etc/sudoers.d/splitify
----

Desde otra terminal local, verificar el acceso:

[source,bash]
----
ssh splitify@<IP>
# Debe entrar sin pedir password
sudo whoami
# Debe imprimir "root" sin pedir password
----

==== 11.12.7 Deshabilitar login de root y password auth por SSH

Ubuntu 24.04 tiene overrides en `/etc/ssh/sshd_config.d/*.conf` que suelen setear `PasswordAuthentication yes` y pisan el archivo principal. Hay que ajustar ambos:

[source,bash]
----
sudo sed -i 's/^#*PermitRootLogin .*/PermitRootLogin no/' /etc/ssh/sshd_config
sudo sed -i 's/^#*PasswordAuthentication .*/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo sed -i 's/^#*PubkeyAuthentication .*/PubkeyAuthentication yes/' /etc/ssh/sshd_config
sudo sed -i 's/^PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config.d/*.conf 2>/dev/null || true
----

Validar la config y aplicar:

[source,bash]
----
sudo sshd -t                     # si sale error, NO reiniciar
sudo systemctl restart ssh
----

*Dejar la sesión actual abierta como seguro*, y desde otra terminal verificar:

[source,bash]
----
ssh splitify@<IP>   # debe entrar con llave
ssh root@<IP>       # debe decir "Permission denied (publickey)" sin pedir password
----

==== 11.12.8 Instalar Java 25 (Eclipse Temurin)

Ubuntu 24.04 no trae Java 25 en sus repos; se usa el repo oficial de Adoptium:

[source,bash]
----
sudo apt install -y wget apt-transport-https gpg
wget -qO - https://packages.adoptium.net/artifactory/api/gpg/key/public | sudo gpg --dearmor -o /usr/share/keyrings/adoptium.gpg
echo "deb [signed-by=/usr/share/keyrings/adoptium.gpg] https://packages.adoptium.net/artifactory/deb $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/adoptium.list
sudo apt update
sudo apt install -y temurin-25-jdk
java -version   # debe mostrar 25.x
----

==== 11.12.9 Instalar Caddy

[source,bash]
----
sudo apt install -y debian-keyring debian-archive-keyring apt-transport-https curl
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' | sudo tee /etc/apt/sources.list.d/caddy-stable.list
sudo apt update
sudo apt install -y caddy
systemctl status caddy   # debe decir "active (running)"
----

==== 11.12.10 Crear el directorio de la app y el archivo de entorno

[source,bash]
----
sudo mkdir -p /opt/splitify
sudo chown splitify:splitify /opt/splitify
sudo chmod 755 /opt/splitify
----

Crear `/etc/splitify.env` con los secretos:

[source,bash]
----
sudo nano /etc/splitify.env
----

Contenido (reemplazar placeholders):

[source]
----
SPOTIFY_CLIENT_ID=...
SPOTIFY_CLIENT_SECRET=...
OPENAI_API_KEY=...
SPRING_PROFILES_ACTIVE=prod
----

Bloquear lectura a otros usuarios:

[source,bash]
----
sudo chown root:splitify /etc/splitify.env
sudo chmod 640 /etc/splitify.env
----

==== 11.12.11 Crear la unit de systemd

[source,bash]
----
sudo nano /etc/systemd/system/splitify.service
----

Contenido:

[source,ini]
----
[Unit]
Description=Splitify Spring Boot application
After=network.target

[Service]
Type=simple
User=splitify
Group=splitify
WorkingDirectory=/opt/splitify
EnvironmentFile=/etc/splitify.env
ExecStart=/usr/bin/java -Xmx512m -jar /opt/splitify/splitify.jar
SuccessExitStatus=143
Restart=on-failure
RestartSec=10

NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ProtectHome=read-only
ReadWritePaths=/opt/splitify

[Install]
WantedBy=multi-user.target
----

Registrar (sin arrancar aún — todavía no hay JAR):

[source,bash]
----
sudo systemctl daemon-reload
sudo systemctl enable splitify
----

==== 11.12.12 Escribir el Caddyfile

[source,bash]
----
sudo cp /etc/caddy/Caddyfile /etc/caddy/Caddyfile.backup
sudo nano /etc/caddy/Caddyfile
----

Contenido:

[source]
----
{
    email tu@email.com
}

splitifyapp.app, www.splitifyapp.app {
    reverse_proxy localhost:8080
    encode gzip
}
----

Validar y recargar:

[source,bash]
----
sudo caddy validate --config /etc/caddy/Caddyfile
sudo systemctl reload caddy
----

En el browser, `https://splitifyapp.app` debe mostrar 502 Bad Gateway con el candado verde (HTTPS OK, solo falta la app). Caddy generó el certificado en el primer request vía ACME HTTP-01.

==== 11.12.13 Configuración del lado del repo

Registrar el redirect URI de producción en el Spotify Developer Dashboard (`https://developer.spotify.com/dashboard` → tu app → Settings → Redirect URIs):

[source]
----
https://splitifyapp.app/login/oauth2/code/spotify
----

Crear en el repo `src/main/resources/application-prod.properties`:

[source,properties]
----
spring.datasource.url=jdbc:sqlite:/opt/splitify/splitify.db?journal_mode=WAL&synchronous=NORMAL
spring.security.oauth2.client.registration.spotify.redirect-uri=https://splitifyapp.app/login/oauth2/code/spotify
server.forward-headers-strategy=native
----

El último es crítico: sin él, Spring genera URLs de redirect OAuth con `http://` porque ve que la request entrante es HTTP (Caddy termina el TLS antes del proxy), y Spotify rechaza el redirect como inválido.

==== 11.12.14 Primer deploy

Desde la raíz del repo en la máquina del desarrollador:

[source,bash]
----
./deploy.sh
----

El script compila el JAR, lo sube por SCP a `/opt/splitify/splitify.jar` y reinicia el servicio. Tras ~3-5 min, `https://splitifyapp.app` muestra la app funcionando.

Verificar desde el server:

[source,bash]
----
sudo systemctl status splitify              # Active: active (running)
sudo journalctl -u splitify -n 50 --no-pager   # logs recientes
----

==== 11.12.15 Activar CI/CD (opcional pero recomendado)

Generar una llave SSH *dedicada* para CI en la máquina del desarrollador:

[source,bash]
----
ssh-keygen -t ed25519 -f ~/.ssh/github_actions_splitify -C "github-actions@splitify" -N ""
----

`-N ""` la deja sin passphrase (el runner no puede responder prompts). Agregar la pública al server:

[source,bash]
----
cat ~/.ssh/github_actions_splitify.pub   # copiar la línea
# En la sesión SSH al server como splitify:
echo "ssh-ed25519 AAAA... github-actions@splitify" >> ~/.ssh/authorized_keys
----

En GitHub → repo → *Settings → Secrets and variables → Actions → New repository secret*, crear tres:

|===
|Nombre |Contenido

|`SSH_PRIVATE_KEY` |Contenido completo de `~/.ssh/github_actions_splitify` (desde `-----BEGIN...` hasta `-----END...-----` inclusive)
|`SSH_HOST` |La `<IP>` del server
|`SSH_USER` |`splitify`
|===

A partir de acá, cada push a `master` dispara el workflow `.github/workflows/deploy.yml` y re-deploya automáticamente.

==== 11.12.16 Resumen visual del orden

[source]
----
[Dev local]                    [Linode Cloud Panel]            [Cloudflare]
generate SSH key ─────────┐
                          │    create Cloud Firewall
                          └──▶ create Linode (pega key)
                                         │
                                         ▼
                                      <IP>
                                         │
                                         └──────────▶ DNS A records @ y www
                                                      (proxy off)
[Dev local]                                                │
ssh root@<IP> ◀───────────────────────────────────────────┘
  ├─ apt update/upgrade
  ├─ adduser splitify + sudo + SSH key + NOPASSWD
  └─ disable root login + PasswordAuthentication

[Dev como splitify]
sudo apt install temurin-25-jdk
sudo apt install caddy
sudo mkdir /opt/splitify
sudo nano /etc/splitify.env
sudo nano /etc/systemd/system/splitify.service
sudo nano /etc/caddy/Caddyfile
sudo systemctl daemon-reload && enable splitify && reload caddy

[Repo]
add Redirect URI en Spotify Dashboard
add src/main/resources/application-prod.properties
add deploy.sh
./deploy.sh   → primer deploy
add .github/workflows/deploy.yml
add secrets en GitHub → push → CI/CD activo
----

'''

== 12. Historial de Cambios

|===
|Fecha |Versión |Descripción

|2026-03-30 |1.0 |Versión inicial del documento. Stack: React (JS) + Tailwind CSS + React Router v7, Spring Boot, SQLite/PostgreSQL, OpenAI Java SDK (GPT-4.1-nano).
|2026-03-31 |1.1 |Nuevo endpoint de sincronización (POST /api/playlists/sync). Paginación en endpoint de canciones. Nuevo endpoint de filtros disponibles (GET /api/playlists/available-filters). Idiomas dinámicos en ClassificationService. Pantalla de sincronización bloqueante y estado vacío en frontend. Carga por scroll en vista de detalle. Filtros dinámicos en creación personalizada.
|2026-04-01 |1.2 |Implementación de sincronización de playlists (RF1.1). Nueva entidad Playlist para caché local de playlists de Spotify. Actualización de endpoints de Spotify API: /tracks renombrado a /items, campo track renombrado a item (cambio de Spotify feb 2026). Documentación de restricciones de Development Mode de Spotify. CSRF deshabilitado para /api/**. SpaController con soporte para rutas anidadas. Scopes actualizados con playlist-read-collaborative. Estructura del proyecto actualizada con archivos implementados. Estado de implementación añadido a endpoints y scopes.
|2026-04-07 |1.3 |Campos multi-valor en Song: `artist` → `artists` (JSON array, soporta colaboraciones), `language` → `languages` (JSON array, soporta canciones multilingües). Nuevos campos agregados en Playlist: `languages`, `genres`, `artists`, `decades` (JSON arrays) para filtros dinámicos. Endpoint available-filters actualizado para incluir artistas. Página de creación personalizada actualizada con filtro de artista. Estrategia de persistencia actualizada con JSON arrays y campos agregados.
|2026-04-08 |1.4 |Nuevo endpoint `GET /api/user/me` para obtener perfil del usuario (nombre y foto de Spotify via `GET /me`). Nuevo `UserController`, `UserProfileDto` y record `UserProfileResponse` en `SpotifyDto`. Frontend: componente `Header.jsx` reutilizable extraído de las páginas. `App.jsx` refactorizado con `AuthLayout` (React Router Outlet) que centraliza la obtención del perfil de usuario, el header y el logout. Eliminada duplicación de header en `MainPage` y `PlaylistDetailPage`.
|2026-04-08 |1.5 |Implementación de RF3 (metadatos de canciones). Nuevo `ClassificationService`: clasificación de género e idioma via ChatGPT API (GPT-4.1-nano, comunicación via RestClient). Fechas de lanzamiento: Spotify provee fecha por defecto, MusicBrainz API corrige con fecha original de primera grabación (sección 7.3). Nuevos campos en Song: `albumName`, `releaseDate`. Dependencia `jackson-databind` agregada. Caché de clasificaciones en re-sincronización (preserva datos por `spotifyId`). Login page actualizada: características reflejan funciones implementadas.
|2026-04-08 |1.6 |Implementación de RF4 (por idioma), RF5 (por género), RF6 (por fecha) y RF6.1 (combinado). Endpoint unificado `POST /api/playlists/create/combined` que acepta combinación de criterios. Canciones deduplicadas por spotifyId. Canciones multilingües se incluyen en todas las playlists de idiomas correspondientes (RN7). Todas las playlists ordenadas por fecha de lanzamiento (RN1). Frontend: botón "Crear Playlists" del OrganizeModal conectado al backend.
|2026-04-09 |1.7 |Eliminada integración con MusicBrainz API. Fechas de lanzamiento ahora exclusivamente de Spotify. Eliminada sección 7.3 (MusicBrainz). Eliminado campo `decades` de Playlist. ClassificationService simplificado (solo ChatGPT). Filtro de décadas eliminado del modal Personalizado (RF7). Actualizado modelo de datos y estrategia de persistencia.
|2026-04-09 |1.8 |Nueva entidad `SplitifyPlaylistSource` para rastrear playlists origen de cada playlist de Splitify. Nuevos campos en Playlist: `filterType`, `filterValue` y `snapshotId` para recordar el criterio de organización y detectar cambios en sync incremental. Nota de desarrollo: sincronización limitada a 10 canciones por playlist (la versión final traerá todas). Endpoints de refresh actualizados con descripción detallada (RF2.2). Estrategia de persistencia actualizada con nueva entidad y nota de desarrollo. Sync incremental implementado: compara `snapshotId` de Spotify para evitar re-sincronizar playlists sin cambios.
|2026-04-09 |1.9 |Refresh de playlists Splitify reescrito: ahora lee el estado real de la playlist en Spotify para detectar cambios manuales (canciones agregadas/eliminadas directamente). Usa `PUT /playlists/{id}/items` para reemplazar el contenido completo y corregir el orden por fecha. Campo `next` agregado a `TracksResponse` para paginación. Nuevo método `fetchSpotifyPlaylistTracks` para leer todas las canciones de una playlist. Nuevo método `replaceSpotifyPlaylistTracks` para reemplazar contenido. ClassificationService: clasificación en lotes de 20 con reintento (2 intentos). Nueva entidad `SongClassification` para caché global de clasificaciones. Eliminado caché in-memory de SpotifyService. Verificación de playlists Splitify eliminadas de Spotify durante el sync.
|2026-04-09 |2.0 |Nuevos campos en Song: `excluded` (recuerda decisiones de no-restaurar permanentemente) y `manuallyAdded` (preserva canciones agregadas manualmente por el usuario en Spotify). Nuevo endpoint `GET /api/playlists/splitify/{id}/refresh/preview` para vista previa de cambios antes de actualizar. Lógica de refresh corregida: canciones eliminadas de playlists origen ahora se eliminan de la playlist Splitify; canciones manuales se preservan permanentemente. Playlist ordering: consulta con `ORDER BY CASE WHEN spotifyId = 'liked_songs' THEN 0 ELSE 1 END, id ASC` para orden consistente. Frontend: re-sincronización ahora actualiza también la lista de playlists Splitify. Estructura del proyecto actualizada: entidades `SongClassification` y `SplitifyPlaylistSource`, repositorios y DTOs faltantes añadidos. Servicio `PlaylistService` eliminado de documentación (no existe, toda la lógica está en `SpotifyService`). Campos inexistentes `languages`, `genres`, `artists` eliminados de entidad Playlist en documentación.
|2026-04-16 |2.1 |Nuevos campos en Playlist: `customLanguages`, `customGenres`, `customArtists` (TEXT, JSON arrays) — persisten los filtros usados al crear playlists personalizadas (RF7), permitiendo re-aplicarlos en refresh (antes se perdían y se traían todas las canciones de las fuentes). Refactor de refresh: `restoreRemoved` (boolean) → `restoredSongIds` (lista de spotifyIds); ahora los endpoints de refresh toman body JSON. Nuevos endpoints: `POST /create/combined/preview` + `POST /create/combined/confirm` (flujo 2-pasos con nombres editables para RF6.1), `POST /batch/refresh/preview` (preview agrupado para batch), `PUT /splitify/{id}/rename`, `PUT /splitify/{id}/image` (RF2.3). Nuevos DTOs: `PlaylistCreateSpec`, `CreatePlaylistsFromSpecsRequest`, `RefreshRequest`, `BatchRefreshRequest`, `BatchRefreshPreviewDto`. Métodos nuevos en `SpotifyApiClient`: `fetchPlaylist`, `renamePlaylist`, `uploadPlaylistImage`. `syncPlaylists` ahora también sincroniza metadata (nombre, imagen) de playlists Splitify sin tocar canciones; `refreshSplitifyPlaylist` también las sincroniza. Frontend: nuevos componentes `ConfirmPlaylistsModal` (nombres editables), `RefreshConfirmModal` (checkboxes por canción + agrupación por playlist en batch). `SplitifyCard` ahora permite renombrar inline con ícono de lápiz siempre visible y cambiar foto mediante overlay al pasar el cursor (imagen redimensionada a 640px JPEG en el cliente). Nuevo scope OAuth: `ugc-image-upload`. Scroll propagation en modales arreglado con `overscroll-contain`.
|2026-04-20 |2.2 |Primer despliegue productivo en Linode (`splitifyapp.app`). Sección 11 (Despliegue) reescrita completa con topología real: Cloudflare DNS (proxy off) → Linode Cloud Firewall (solo 22/80/443) → Caddy (HTTPS via Let's Encrypt) → Spring Boot en `localhost:8080` como servicio systemd → SQLite WAL en `/opt/splitify/`. Nuevas subsecciones: 11.1 topología con diagrama, 11.2 infraestructura (Nanode 1GB, Ubuntu 24.04), 11.3 hardening (usuario no-root, SSH solo por llave, systemd `ProtectSystem`/`NoNewPrivileges`), 11.4 perfiles con `application-prod.properties`, 11.5 env vars (`SPRING_PROFILES_ACTIVE=prod`; se elimina `DATABASE_URL` porque ya no se usa Postgres), 11.6 archivos clave en el server, 11.7 Caddyfile y flujo ACME, 11.8 runbook de deploy con `deploy.sh`, 11.9 troubleshooting, 11.10 rollback. Stack tecnológico actualizado: BD de prod pasa de PostgreSQL a SQLite con WAL mode (suficiente para el perfil de uso). Nuevas filas: Caddy v2 (reverse proxy + HTTPS automático) y systemd (gestor de servicios). Estructura del proyecto: agregado `deploy.sh` en la raíz y `application-prod.properties` en resources. Nuevo archivo `application-prod.properties` con: URL SQLite absoluta en `/opt/splitify/splitify.db` con `journal_mode=WAL&synchronous=NORMAL`, redirect URI `https://splitifyapp.app/login/oauth2/code/spotify`, `server.forward-headers-strategy=native` (crítico para que OAuth funcione detrás de Caddy).
|2026-04-20 |2.3 |CI/CD agregado. Nueva subsección 11.11 que describe el pipeline de GitHub Actions (`.github/workflows/deploy.yml`): en cada push a `master` compila el JAR en un runner `ubuntu-latest` (JDK 25 Temurin + Node 20), lo sube por SCP al server y reinicia el servicio systemd. Requiere tres secretos en el repo (`SSH_PRIVATE_KEY`, `SSH_HOST`, `SSH_USER`) y una llave SSH dedicada para CI (independiente de la personal). Concurrencia limitada a una corrida (`concurrency: deploy-prod`) con `cancel-in-progress: false` para evitar carreras. `deploy.sh` se conserva para deploys manuales de emergencia. Estructura del proyecto: agregado `.github/workflows/deploy.yml`.
|2026-04-21 |2.4 |Nueva subsección 11.12 "Runbook de Provisioning (First-Time Setup)" con todos los comandos necesarios para levantar el stack desde cero en un server nuevo, en orden cronológico. Cubre 16 sub-pasos: pre-requisitos del desarrollador (SSH key local), creación de Cloud Firewall en Linode, creación de la instancia Ubuntu 24.04, configuración de DNS en Cloudflare (proxy off), primer login y update del sistema, creación del usuario de servicio `splitify` con sudo NOPASSWD, deshabilitación de root login y password auth (manejando overrides en `/etc/ssh/sshd_config.d/`), instalación de Java 25 Temurin desde repo Adoptium, instalación de Caddy desde repo oficial, creación de `/opt/splitify` y `/etc/splitify.env` con permisos `640`, redacción del `splitify.service` de systemd con hardening, redacción del `Caddyfile`, registro del Redirect URI de prod en Spotify Dashboard, creación de `application-prod.properties`, primer deploy con `deploy.sh`, y activación opcional de CI/CD con llave SSH dedicada. Incluye comandos exactos con explicación de cada uno, y un diagrama visual del orden entre Dev local / Linode Cloud Panel / Cloudflare / server.
|===
